logLevel: info

# RTMP ingest
rtmp: true

# RTSP serve
rtsp: true

# HTTP status/metrics
http:
  enabled: yes
  listen: :8080

# auto-publish every RTMP app/stream on the same RTSP path
paths:
  all:
    source: publisher
