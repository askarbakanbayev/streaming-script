# mediamtx.yml

logLevel: info

# simply turn RTMP ingest on/off
rtmp: true

# simply turn RTSP serve on/off
rtsp: true

paths:
  # every incoming RTMP <app>/<stream> is auto-published at the same path over RTSP
  all:
    source: publisher
