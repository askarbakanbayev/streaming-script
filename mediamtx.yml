# уровень лога
logLevel: info

# включаем RTMP-приём
rtmp: true

# включаем RTSP-отдачу
rtsp: true

# все потоки, опубликованные по RTMP/RTSP, автоматически
# будут доступны на том же пути
paths:
  all:
    source: publisher
